name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  lint-typecheck-test-build:
    runs-on: ubuntu-latest
    env:
      # Dummy values so `next build` can statically analyze pages that
      # reference these at module scope. No real Supabase project is
      # contacted during build/typecheck/unit-tests — pages that hit the
      # network are all marked dynamic (see IMPLEMENTATION_PLAN.md) and
      # only actually run at request time.
      NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
      SUPABASE_SERVICE_ROLE_KEY: placeholder-service-role-key
      NEXT_PUBLIC_APP_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm run test
        # Integration tests inside `npm run test` self-skip without live
        # Supabase credentials (SUPABASE_SERVICE_ROLE_KEY etc. pointing at
        # a real project) — see tests/integration and docs/testing.md.

      - name: Build
        run: npm run build
